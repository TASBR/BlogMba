@model BlogMBA.Business.Models.Comentarios.Comentario

@{
    ViewData["Title"] = "Adicionar Comentário";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Adicionando</h1>

<h4>Comentario</h4>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
          @*   <div class="form-group">
                <label asp-for="PostId" class="control-label"></label>
                <select asp-for="PostId" class ="form-control" asp-items="ViewBag.PostId"></select>
            </div> *@
            <div class="form-group">
                <label asp-for="Texto" class="control-label"></label>
                <input asp-for="Texto" class="form-control" />
                <span asp-validation-for="Texto" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Enviar" asp-route-postId=@ViewBag.postId class="btn btn-info" />
                <a asp-action="Details" asp-controller="Posts" asp-route-id=@ViewBag.postId class="btn btn-warning" asp>Voltar</a>
            </div>
        </form>
    </div>
</div>

@* <div>
    <a asp-action="Index">Back to List</a>
</div> *@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
